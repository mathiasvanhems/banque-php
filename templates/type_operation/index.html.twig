{% extends 'base.html.twig' %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ page_name }}! ✅</h1>
    <a href="{{ path('type_edit') }}">Create</a>
    <table class='t1'>
        <thead>
            <tr>
                <th colspan="4">Liste des Types d'opération</th>
            </tr>
            <tr>
                <th colspan="1">Libelle</th>
                <th colspan="1">Récurrence</th>
                <th colspan="1">Entrée/Sortie</th>
                <th colspan="1">Action</th>
            </tr>
        </thead>
        <tbody>
            {% for type in types %}
                <tr>
                    <td>{{ type.libelle}}</td>
                    <td>{{type.recurrence}}</td>
                    <td>{% if type.sortie == true %}Sortie 🟥 {% else %} Entrée ✅ {% endif %}</td>
                    <td><a href="{{ path('type_edit', {'id':type.id}) }}">edit</a> / <a href="{{ path('type_delete', {'id':type.id}) }}">delete</a></td>
                </tr>
            {% endfor %}
            
        </tbody>
    </table>

</div>
{% endblock %}
