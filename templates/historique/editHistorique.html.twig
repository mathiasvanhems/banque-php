{% extends 'base.html.twig' %}

{% block title %}Hello HistoriqueController!{% endblock %}

{% block body %}
<div class="example-wrapper">


    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    <br />
    <p><a href="{{ path('app_historique') }}"class="btn btn-secondary btn-sm" style="display: inline-block">🔙 Retour</a></p>
</div>
{% endblock %}
