{% extends 'base.html.twig' %}

{% block title %}Hello BanqueController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/banque/src/Controller/BanqueController.php'|file_link(0) }}">src/Controller/BanqueController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/banque/templates/banque/index.html.twig'|file_link(0) }}">templates/banque/index.html.twig</a></code></li>
        <div {{ stimulus_controller('say-hello') }}>
        <input type="text" {{ stimulus_target('say-hello', 'name') }}>

            <button {{ stimulus_action('say-hello', 'greet') }}>
                Greet
            </button>

        <div {{ stimulus_target('say-hello', 'output') }}></div></div>

    </ul>
     {{ render_chart(chart) }}

    {# You can pass HTML attributes as a second argument to add them on the <canvas> tag #}
    {# {{ render_chart(chart, {'class': 'my-chart'}) }}#}



</div>

{% endblock %}
